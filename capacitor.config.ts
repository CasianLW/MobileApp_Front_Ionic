import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MobileApp_Front_Ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
